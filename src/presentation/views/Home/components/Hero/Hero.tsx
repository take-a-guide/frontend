import { HeroStyles } from './styles';

export const Hero: React.FC = () => {
  return (
    <HeroStyles.Container>
      <HeroStyles.Content>
        <HeroStyles.Title>Find your next home</HeroStyles.Title>
      </HeroStyles.Content>
    </HeroStyles.Container>
  );
};
